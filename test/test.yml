---

- hosts: all
  become: 'yes'
  vars:
    project: 'test'
  roles:
    - role: ansible-role-mysql
      mysql_databases:
        - name: '{{ project }}'
          state: present
        - name: '{{ project }}'
          path: /vagrant/storage/mysqlsampledatabase.sql
      mysql_users:
        - name: '{{ project }}'
          privs:
            - '{{ project }}.*:ALL'
          hosts:
            - localhost
